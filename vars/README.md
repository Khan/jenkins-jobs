This directory holds global variables usable by Groovy (pipeline)
jenkins jobs.

Note that for pipelines, global "variables" includes custom step
commands.

See
   https://jenkins.io/doc/book/pipeline/shared-libraries/
